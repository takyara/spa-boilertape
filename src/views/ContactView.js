/* =============================================
   CONTACTVIEW.JS
   ============================================= */

const ContactView = {
  async render(params) {
    return `
      <section class="container">
        <h1>Contact</h1>
        <p>Un formulaire de contact viendra ici.</p>
      </section>
    `;
  },

  afterRender() {},
};

export default ContactView;
